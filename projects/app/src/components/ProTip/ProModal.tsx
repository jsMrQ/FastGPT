type ProModalProps = {
  isOpen?: boolean;
  onClose?: () => void;
  forceShow?: boolean;
};

/** 社区二开：不展示商业版升级弹窗。 */
const ProModal = (_props: ProModalProps) => {
  return null;
};

export default ProModal;
